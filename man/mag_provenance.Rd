% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mag_provenance.R
\name{mag_provenance}
\alias{mag_provenance}
\title{MAG provenance}
\usage{
mag_provenance(mag_id, path = NULL, db = "gopheR_db.sqlite", read_only = TRUE)
}
\arguments{
\item{mag_id}{Character scalar. MAG identifier (without the \verb{mag:} prefix).}

\item{path}{Character scalar. Directory containing the database file.
If \code{NULL}, uses \code{getOption("gopheR.db_path")} or \code{Sys.getenv("GOPHER_DB_PATH")}.}

\item{db}{Character scalar. Database filename (default \code{"gopheR_db.sqlite"}).}

\item{read_only}{Logical. If \code{TRUE} (default), attempts to open the SQLite database
in read-only mode.}
}
\value{
A data frame describing MAG provenance (assembly/readset/sample context).
}
\description{
Returns provenance information for a MAG, linking it to the assembly it was binned
from and the read sets (and samples) that contributed to that assembly.
}
\details{
This function opens and closes the database connection automatically. For advanced
use (e.g., batching multiple queries on a single connection), see
\code{\link[=mag_provenance_con]{mag_provenance_con()}}.
}
