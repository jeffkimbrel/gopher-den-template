% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_templates.R
\name{write_templates}
\alias{write_templates}
\title{Write Excel templates for data entry}
\usage{
write_templates(
  out_dir,
  tables = NULL,
  path = NULL,
  db = "gopheR_db.sqlite",
  read_only = TRUE,
  exclude_cols = c("created_at", "updated_at", "row_hash", "stable_hash"),
  edge_types = NULL,
  edge_include_helper_cols = TRUE,
  n_blank_rows = 50
)
}
\arguments{
\item{out_dir}{Directory to write templates into.}

\item{tables}{Character vector of table names. If NULL, uses all DB tables
except internal SQLite tables.}

\item{path, db, read_only}{Passed to \code{with_gopher_con()}.}

\item{exclude_cols}{Column names to omit (system-managed / derived).}

\item{edge_types}{Optional character vector of allowed \code{edge_type} values.
Used to add an Excel dropdown validation in the edge template.}

\item{edge_include_helper_cols}{Logical; if TRUE, adds helper columns to the
edge template (child_type/child_id/parent_type/parent_id) to reduce UID typos.}

\item{n_blank_rows}{Number of blank rows to pre-allocate in each sheet.}
}
\value{
Invisibly returns vector of file paths written.
}
\description{
Creates a folder of Excel templates (one workbook per table) derived from the
database schema. Intended for users to fill and return for QC + ingestion.
}
